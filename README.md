# MyWPBlog
# Setup WP Blog using Docker
Wordpress blog using Docker.  Itincludes the Dockerfiles for all the containers.
I am adding Docker file for php-fpm and nginx taking the help from docker wordpress official image. I am also keeping wp-config.php file and docker-compose.yml file that helps me to run container using docker compose
